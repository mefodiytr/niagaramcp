# v0.5.1 implementation notes

**Branch:** `v0.5.1-write-tools` — **stacked on `v0.5-user-context`**
(unmerged). When v0.5 merges to main, this branch rebases onto
post-merge main; commit hashes change but content stays identical.
Documented per "<неделя — стэкуем" policy from the design pass.
**Commits on branch:** 8.
**Date:** 2026-05-10.
**Build status:** all 8 commits — `:niagaramcp-rt:compileJava` green.
`:assemble` not retried (recurring station-lock — same as v0.4 / v0.4.1
/ v0.5 notes). Final jar size: not measured this branch; v0.5 was
~230 KiB and the +9 tools / +25 LOC of error codes / smoke add ≈ 8 KB
estimated → ≈238 KiB.

---

## Diff vs v0.5 tip

```
10 files changed, 1662 insertions(+), 5 deletions(-)
```

| Area | Files | Δ |
|---|---:|---|
| `server/tools/` (+6 new tools — Add/Commit/Invoke/Link/Remove/Set/Unlink) | 7 | +1 431 |
| `server/McpProtocol.java` (4 new error codes) | 1 | +25 |
| `server/BMcpPlatformService.java` (registrations + imports) | 1 | +15 |
| `clients/python/niagaramcp_smoke.py` (run_v051_tests + flags) | 1 | +191 / −5 |

## Class-count + tool-count delta

| | v0.5 | v0.5.1 |
|---|---:|---:|
| Java source files in `com.niagaramcp.server[...]` | 70 | **77 (+7: 7 new tool classes)** |
| Class files in built jar | 120 | **~127–130 estimated** |
| Tools in `tools/list` | 38 | **45 (+7)** |
| Resources | 6 | 6 |
| Prompts | 7 | 7 |
| Properties on service | 19 | 19 (unchanged) |
| HTTP endpoints | 4 | 4 (unchanged) |
| JSON-RPC error codes (impl-defined) | 11 | **15 (+4: -32013, -32014, -32015, -32016)** |

## The 8 commits

| # | SHA | Subject |
|---|---|---|
| 1 | `e51604c` | feat: removeComponent (DESTRUCTIVE, dryRun + inbound-link safety) |
| 2 | `d21f4db` | feat: setSlot (typed value coerce for BSimple primitives) |
| 3 | `8f9e871` | feat: invokeAction (BSimple parameter coercion) |
| 4 | `da98fb2` | feat: addExtension (parent.add for extension types) |
| 5 | `a5656f0` | feat: linkSlots + unlinkSlots (paired) |
| 6 | `8363a8f` | feat: commitStation (synchronous .bog save under user-Context) |
| 7 | `71b1dc0` | feat: smoke steps 26-31 for write-tools tail |
| 8 | (this) | docs: v0.5.1 implementation notes + SESSIONS + README + CHANGELOG |

Each commit was verified by `:niagaramcp-rt:compileJava` before
proceeding. Commit 7 verified the smoke client via
`py -c "ast.parse(...)"`.

## Surprises during implementation

### `BComplex.getPropertyInParent()` is the right slot-resolver
Initial draft of `removeComponent` used
`parent.getProperty(target.getSlot(target))` which doesn't compose.
Recon caught it before commit; corrected to `target.getPropertyInParent()`.
Same fix applied later in `unlinkSlots`.

### `BStation` exposes both `save()` and `doSave(Context)`
`save()` is the no-arg convenience method (default cx → service
identity). `doSave(Context)` is the impl that subclasses override;
calling it directly with our user-Context propagates user identity
properly. Used the latter in `commitStation` so the audit story
holds. Niagara convention: `xxx()` ↔ `doXxx(Context)`.

### `BLink` is the link target's child slot
The link-data structure is stored as a child slot on the SINK
component (= where data flows TO), not on the source. So
`sink.getLinks()` returns inbound links to that sink. For
`removeComponent`'s safety check, this directly answers
"is anyone reading from me?" without a station walk.

### `LinkCheck` API found cleanly
`BComponent.checkLink(source, sourceSlot, sinkSlot, cx)` returns a
`LinkCheck` with `isValid()` + `getInvalidReason()`. Niagara owns
the type-compatibility predicate, we just surface the reason in
`-32016 ERR_LINK_TYPE_MISMATCH` data.

### Auto-pick converter (Q6) deferred
Walking the converter registry isn't a stable public API. v0.5.1
ships hard "convert: false" semantics (refuse on type mismatch).
v0.5.2 will add explicit `converterType` arg + lookup of
named BTypeConverter — design Q6 already locked.

### Smoke fixtures: createComponent → ... → removeComponent cycle
The v0.5.1 smoke flow creates its own throwaway folder
(`mcpSmoke51_<unix-ts>`), exercises the new tools on it, then
cleans up via the actual removeComponent (dryRun + final). This
proves the tools work end-to-end and ensures smoke leaves no
debris on the test station. addExtension / linkSlots /
unlinkSlots e2e fixtures need station-specific setup (real
extension types, compatible slot types) — deferred to v0.5.2 with
proper helper functions.

### invokeAction + setSlot share the same coercion logic
Both tools coerce JSON primitives to BSimple BValues based on the
existing/expected slot/parameter type. Identical 6-type dispatch
table (BString/BBoolean/BInteger/BLong/BFloat/BDouble) is
duplicated across `SetSlotTool.coerce()` and
`InvokeActionTool.coerceForActionParam()`. A `BValueCoercer`
helper would dedupe this in v0.5.2 — small refactor, not worth
its own commit during the M1-tail sprint.

### Inline schema-helper duplication for nameStrategy
`AddExtensionTool` re-uses the nameStrategy enum schema from
`CreateComponentTool`. Rather than widening either class's package
surface for a shared helper, inlined the makeNameStrategyParam()
copy with a shimmed name. Same dedupe candidate as the BValueCoercer
above — queued for the v0.5.2 cleanup pass.

## Things deferred (with rationale)

| Item | Why deferred |
|---|---|
| Auto-pick BTypeConverter on link type mismatch (`convert: true` + named `converterType`) | Walking the converter registry isn't a stable public API; explicit `converterType` arg locked in design Q6. v0.5.2. |
| `addExtension` type-applicability pre-check (would map to -32015 ERR_EXTENSION_NOT_APPLICABLE which is already declared) | Niagara's "can this Type child this parent Type" predicate API not found in this commit's recon. Currently relies on Niagara's own add()-time validation surfacing as -32603. v0.5.2. |
| `BValueCoercer` helper to dedupe SetSlot ↔ InvokeAction primitive coercion | Cosmetic refactor; not worth its own commit during the M1-tail sprint. v0.5.2. |
| `unlinkSlots` ergonomic args `{sinkOrd, linkName}` (in addition to the current `{linkOrd}`) | Both forms map to the same backing remove(); the linkOrd form is sufficient and matches what `linkSlots` returns directly. v0.5.2 if operator feedback wants the convenience form. |
| Smoke e2e fixtures for addExtension / linkSlots / unlinkSlots | Need station-specific setup (real extension types, compatible slot pairs). Helper function deferred. |
| `clearSlot` tool for nulling/resetting a BSimple slot | Distinct semantic from setSlot (resetting to type default, not setting null). Not part of M1; queued separately. |

## Verification needed (operator runtime)

Per-commit `:compileJava` is green. Before tag/merge:

1. **Stop station**, replace `$niagara_home/modules/niagaramcp-rt.jar`
   with the v0.5.1 build, restart.
2. **`tools/list`** length = **45** (was 38 in v0.5).
   New entries (all `category: "write"`, all `requiresUserContext: true`):
   `removeComponent` (DESTRUCTIVE), `setSlot` (MUTATION),
   `invokeAction` (MUTATION), `addExtension` (MUTATION),
   `linkSlots` (MUTATION), `unlinkSlots` (DESTRUCTIVE),
   `commitStation` (MUTATION).
3. **All 25 v0.5 smoke steps** still pass
   (`py niagaramcp_smoke.py --skip-v051 ...`).
4. **v0.5.1 smoke pre-flight** (same as v0.5):
   - Workbench: BUser `mcpSmokeUser` exists, has add-permission on
     `/Drivers/`, also has remove- and config-write permissions
     (for setSlot + removeComponent + commitStation).
   - `enableTestSetup = true`, restart.
   - `py niagaramcp_smoke.py --smoke-user mcpSmokeUser ...`
     → expect `Result: 31 passed, 0 failed`.
   - Verify `niagaramcp.audit.log` has 6+ fresh JSON lines from
     v0.5.1 steps with sane `tool` / `user` / `durationMs`.
5. **`-32014` test**: under user-Bearer, invokeAction with bogus
   actionName → expect error.code = -32014. (Step 28 covers.)
6. **`-32016` test**: linkSlots with incompatible source/sink types
   (e.g. NumericPoint.out → BooleanWritable.in) → expect
   error.code = -32016 with data.reason. (Manual; not in smoke
   v0.5.1 — needs station-specific points.)
7. **`-32013` test**: removeComponent on a component with inbound
   links AND `force=false` → expect error.code = -32013 with
   data.inboundLinkCount and sampleSourceOrds. (Manual.)
8. **commitStation** Property Sheet inspection: after a setSlot +
   commitStation cycle, restart station and verify the slot value
   persists (proves doSave actually flushed).

## Branch state at end of work

- Branch `v0.5.1-write-tools` has 8 commits, **stacked on
  `v0.5-user-context`** (unmerged stack).
- `main` unchanged (still at v0.4.1 merge).
- `v0.5-user-context` unchanged (unmerged, awaiting review).
- Stack: main (v0.4.1) → v0.5-user-context (11 commits) →
  v0.5.1-write-tools (8 commits).
- Per-commit `:compileJava` green; `:jar` not run end-of-branch
  (recurring station-lock).

## Final summary table

| Metric | Value |
|---|---|
| Commits | 8 |
| Files changed vs v0.5 tip | 10 |
| LOC added (insertions) | 1 662 |
| LOC removed (deletions) | 5 |
| Net LOC delta | **+1 657** |
| New Java files | 7 (Remove/Set/Invoke/AddExt/LinkS/UnlinkS/CommitStation) |
| Deleted Java files | 0 |
| Tools registered | **45** (+7) |
| New properties | 0 |
| New JSON-RPC error codes | **4** (-32013, -32014, -32015, -32016) |
| New runtime threads | 0 |
| New 3rd-party deps | 0 |
| `:compileJava` per commit | all 8 green |
| `:assemble` end-of-branch | not retried (station lock) |
| Backward compat (v0.5 / v0.4.1 / earlier) | preserved by static review (operator runtime confirm via checklist) |
| M1 write-tool set | **complete** (createComponent in v0.5 + 6 in v0.5.1 + commitStation) |
